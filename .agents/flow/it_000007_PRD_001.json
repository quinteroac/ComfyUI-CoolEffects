{
  "goals": [
    "Provide five `pan_*.frag` shaders that implement smooth UV-based pan animations.",
    "Register default uniform values for all five pan effects in `effect_params.py`.",
    "Keep all new uniforms as plain `float` types so the existing uniform-passing loop in `CoolVideoGenerator` requires no changes."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Pan Left shader renders a left-scrolling animation",
      "description": "As a ComfyUI user, I want a `pan_left` shader so that the image scrolls continuously from right to left over the animation duration.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`shaders/glsl/pan_left.frag` exists and compiles without GLSL errors."
        },
        {
          "id": "US-001-AC02",
          "text": "The shader accepts `uniform sampler2D u_image`, `uniform float u_time`, `uniform vec2 u_resolution`, `uniform float u_speed`, `uniform float u_origin_x`, `uniform float u_origin_y`."
        },
        {
          "id": "US-001-AC03",
          "text": "At `u_time = 0` the visible region starts at `(u_origin_x, u_origin_y)`; UV offset increases in the `-X` direction over time proportional to `u_speed * u_time`, wrapping with `fract()`."
        },
        {
          "id": "US-001-AC04",
          "text": "`DEFAULT_PARAMS[\"pan_left\"]` exists in `effect_params.py` with keys `u_speed`, `u_origin_x`, `u_origin_y`."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Pan Right shader renders a right-scrolling animation",
      "description": "As a ComfyUI user, I want a `pan_right` shader so that the image scrolls continuously from left to right.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`shaders/glsl/pan_right.frag` exists and compiles without GLSL errors."
        },
        {
          "id": "US-002-AC02",
          "text": "Accepts the same uniform contract as US-001."
        },
        {
          "id": "US-002-AC03",
          "text": "UV offset increases in the `+X` direction over time, wrapping with `fract()`."
        },
        {
          "id": "US-002-AC04",
          "text": "`DEFAULT_PARAMS[\"pan_right\"]` exists in `effect_params.py` with keys `u_speed`, `u_origin_x`, `u_origin_y`."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Pan Up shader renders a bottom-to-top scrolling animation",
      "description": "As a ComfyUI user, I want a `pan_up` shader so that the image scrolls from bottom to top.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`shaders/glsl/pan_up.frag` exists and compiles without GLSL errors."
        },
        {
          "id": "US-003-AC02",
          "text": "Accepts the same uniform contract as US-001."
        },
        {
          "id": "US-003-AC03",
          "text": "UV offset increases in the `+Y` direction over time, wrapping with `fract()`."
        },
        {
          "id": "US-003-AC04",
          "text": "`DEFAULT_PARAMS[\"pan_up\"]` exists in `effect_params.py` with keys `u_speed`, `u_origin_x`, `u_origin_y`."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Pan Down shader renders a top-to-bottom scrolling animation",
      "description": "As a ComfyUI user, I want a `pan_down` shader so that the image scrolls from top to bottom.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`shaders/glsl/pan_down.frag` exists and compiles without GLSL errors."
        },
        {
          "id": "US-004-AC02",
          "text": "Accepts the same uniform contract as US-001."
        },
        {
          "id": "US-004-AC03",
          "text": "UV offset increases in the `-Y` direction over time, wrapping with `fract()`."
        },
        {
          "id": "US-004-AC04",
          "text": "`DEFAULT_PARAMS[\"pan_down\"]` exists in `effect_params.py` with keys `u_speed`, `u_origin_x`, `u_origin_y`."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Pan Diagonal shader renders a diagonal scrolling animation",
      "description": "As a ComfyUI user, I want a `pan_diagonal` shader so that the image scrolls diagonally with a configurable angle.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "`shaders/glsl/pan_diagonal.frag` exists and compiles without GLSL errors."
        },
        {
          "id": "US-005-AC02",
          "text": "Accepts the same uniform contract as US-001 plus `uniform float u_dir_x` and `uniform float u_dir_y` to control the diagonal angle (defaults `0.7071` each for 45°)."
        },
        {
          "id": "US-005-AC03",
          "text": "UV offset is `u_speed * u_time * vec2(u_dir_x, u_dir_y)`, wrapping with `fract()`."
        },
        {
          "id": "US-005-AC04",
          "text": "`DEFAULT_PARAMS[\"pan_diagonal\"]` exists in `effect_params.py` with keys `u_speed`, `u_origin_x`, `u_origin_y`, `u_dir_x`, `u_dir_y`."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "Each of the five `.frag` files must satisfy the shared uniform contract: `uniform sampler2D u_image`, `uniform float u_time`, `uniform vec2 u_resolution`."
    },
    {
      "id": "FR-2",
      "description": "`u_speed` controls the rate of UV offset per second (e.g. default `0.2` means 20% of image width/height per second)."
    },
    {
      "id": "FR-3",
      "description": "`u_origin_x` and `u_origin_y` define the UV starting offset (range 0.0–1.0, default `0.0` for both)."
    },
    {
      "id": "FR-4",
      "description": "All UV arithmetic must use `fract()` for seamless wrapping — no clamping."
    },
    {
      "id": "FR-5",
      "description": "`merge_params(effect_name, {})` must return a complete set of uniforms for all five pan effects without raising `KeyError`."
    },
    {
      "id": "FR-6",
      "description": "No changes to `video_generator.py` — all new uniforms are plain floats compatible with the existing `float(uniform_value)` cast."
    }
  ]
}
