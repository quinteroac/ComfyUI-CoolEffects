{
  "goals": [
    "Accept IMAGE batches of any size (including batch-of-1) without breaking existing workflows.",
    "Loop the input batch automatically when `round(duration * fps) > batch_size`.",
    "Keep GPU/VRAM usage bounded: upload only one texture per render frame (never all frames at once)."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Single image input remains backward-compatible",
      "description": "As a ComfyUI artist, I want the node to behave identically when I connect a single image (batch of 1) so that my existing workflows are not broken.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "A tensor with shape `[1, H, W, 3]` or `[H, W, 3]` produces the same output as before (all output frames use that single image as texture)."
        },
        {
          "id": "US-001-AC02",
          "text": "No change to `INPUT_TYPES`, `RETURN_TYPES`, or node registration is required for single-image workflows."
        },
        {
          "id": "US-001-AC03",
          "text": "Existing test suite passes without modification."
        },
        {
          "id": "US-001-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "IMAGE batch used as per-frame texture sequence",
      "description": "As a ComfyUI artist, I want to connect a batch of images to the `CoolVideoGenerator` node so that each output frame renders the GLSL shader on top of the corresponding input frame of the batch.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "A tensor with shape `[N, H, W, 3]` is accepted without error."
        },
        {
          "id": "US-002-AC02",
          "text": "Output frame at index `i` uses `batch[i % N]` as the `u_image` texture (modulo loop)."
        },
        {
          "id": "US-002-AC03",
          "text": "The total number of output frames is still `round(duration * fps)` regardless of batch size."
        },
        {
          "id": "US-002-AC04",
          "text": "When `round(duration * fps) <= N`, only the first `round(duration * fps)` batch frames are used (no unnecessary GPU uploads)."
        },
        {
          "id": "US-002-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Batch texture upload is memory-efficient",
      "description": "As a ComfyUI artist running on a GPU with limited VRAM, I want the node to upload only one frame texture per render cycle so that large batches don't exhaust GPU memory.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "Only one ModernGL texture object is alive at any point during the render loop (the previous frame's data is overwritten or the texture is updated in-place via `texture.write()`)."
        },
        {
          "id": "US-003-AC02",
          "text": "No list or array of N ModernGL textures is created upfront."
        },
        {
          "id": "US-003-AC03",
          "text": "A batch of 100+ frames can be processed without OOM errors on a GPU with typical VRAM (tested by inspection / code review)."
        },
        {
          "id": "US-003-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Tests cover the batch rendering path",
      "description": "As a developer, I want automated tests for the batch input path so that regressions are caught before merge.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "A test verifies that output frame `i` corresponds to `batch[i % N]` (mock or stub ModernGL; assert texture `write()` is called with the correct bytes for each frame index)."
        },
        {
          "id": "US-004-AC02",
          "text": "A test verifies that a batch-of-1 tensor produces the same result as the existing single-image path."
        },
        {
          "id": "US-004-AC03",
          "text": "All tests in `tests/` pass with `pytest`."
        },
        {
          "id": "US-004-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** `_extract_input_image` (or a replacement function) must handle a batch tensor `[N, H, W, 3]` and return the full batch as a list/array of N uint8 frames, not just `frame[0]`."
    },
    {
      "id": "FR-2",
      "description": "** In `CoolVideoGenerator.execute`, the render loop must select `batch_frame = frames[frame_index % batch_size]` for each iteration and update the input texture data in-place using `texture.write(batch_frame.tobytes())`."
    },
    {
      "id": "FR-3",
      "description": "** The input texture object must be created once (using dimensions from the first batch frame) and reused across all render iterations; only its pixel data is updated per frame."
    },
    {
      "id": "FR-4",
      "description": "** Width and height must be taken from the first frame of the batch; all frames in the batch are assumed to share the same resolution (no resize step required)."
    },
    {
      "id": "FR-5",
      "description": "** The `image` input type remains `(\"IMAGE\",)` — no schema change needed, as ComfyUI's IMAGE type already supports batches."
    }
  ]
}
