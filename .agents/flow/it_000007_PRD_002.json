{
  "goals": [
    "Expose all five pan effects as first-class ComfyUI nodes with named, typed inputs.",
    "Keep the implementation consistent with the existing dedicated-node pattern.",
    "Provide pytest tests that exercise the full rendering path for each pan direction."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "CoolPanLeftEffect node produces valid EFFECT_PARAMS",
      "description": "As a ComfyUI user, I want a `CoolPanLeftEffect` node so that I can connect it to `CoolVideoGenerator` to render a left-scrolling animation with explicit speed and origin controls.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`nodes/pan_left_effect.py` defines `CoolPanLeftEffect` with inputs `speed` (FLOAT, default 0.2, min 0.0, max 5.0, step 0.05), `origin_x` (FLOAT, default 0.0, min 0.0, max 1.0, step 0.01), `origin_y` (FLOAT, default 0.0, min 0.0, max 1.0, step 0.01)."
        },
        {
          "id": "US-001-AC02",
          "text": "`RETURN_TYPES = (\"EFFECT_PARAMS\",)` and `CATEGORY = \"CoolEffects\"`."
        },
        {
          "id": "US-001-AC03",
          "text": "`execute()` returns `build_effect_params(\"pan_left\", {\"u_speed\": speed, \"u_origin_x\": origin_x, \"u_origin_y\": origin_y})`."
        },
        {
          "id": "US-001-AC04",
          "text": "Node is listed under `NODE_CLASS_MAPPINGS[\"CoolPanLeftEffect\"]` in `__init__.py`."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "CoolPanRightEffect node produces valid EFFECT_PARAMS",
      "description": "As a ComfyUI user, I want a `CoolPanRightEffect` node so that I can render a right-scrolling animation.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`nodes/pan_right_effect.py` defines `CoolPanRightEffect` with the same input schema as US-001."
        },
        {
          "id": "US-002-AC02",
          "text": "`execute()` returns `build_effect_params(\"pan_right\", {\"u_speed\": speed, \"u_origin_x\": origin_x, \"u_origin_y\": origin_y})`."
        },
        {
          "id": "US-002-AC03",
          "text": "Node is listed under `NODE_CLASS_MAPPINGS[\"CoolPanRightEffect\"]` in `__init__.py`."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "CoolPanUpEffect node produces valid EFFECT_PARAMS",
      "description": "As a ComfyUI user, I want a `CoolPanUpEffect` node so that I can render a bottom-to-top scrolling animation.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`nodes/pan_up_effect.py` defines `CoolPanUpEffect` with the same input schema as US-001."
        },
        {
          "id": "US-003-AC02",
          "text": "`execute()` returns `build_effect_params(\"pan_up\", {\"u_speed\": speed, \"u_origin_x\": origin_x, \"u_origin_y\": origin_y})`."
        },
        {
          "id": "US-003-AC03",
          "text": "Node is listed under `NODE_CLASS_MAPPINGS[\"CoolPanUpEffect\"]` in `__init__.py`."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "CoolPanDownEffect node produces valid EFFECT_PARAMS",
      "description": "As a ComfyUI user, I want a `CoolPanDownEffect` node so that I can render a top-to-bottom scrolling animation.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`nodes/pan_down_effect.py` defines `CoolPanDownEffect` with the same input schema as US-001."
        },
        {
          "id": "US-004-AC02",
          "text": "`execute()` returns `build_effect_params(\"pan_down\", {\"u_speed\": speed, \"u_origin_x\": origin_x, \"u_origin_y\": origin_y})`."
        },
        {
          "id": "US-004-AC03",
          "text": "Node is listed under `NODE_CLASS_MAPPINGS[\"CoolPanDownEffect\"]` in `__init__.py`."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "CoolPanDiagonalEffect node produces valid EFFECT_PARAMS",
      "description": "As a ComfyUI user, I want a `CoolPanDiagonalEffect` node so that I can render a diagonal scrolling animation with a configurable angle.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "`nodes/pan_diagonal_effect.py` defines `CoolPanDiagonalEffect` with inputs `speed` (FLOAT, default 0.2, min 0.0, max 5.0, step 0.05), `origin_x`, `origin_y` (same as US-001), `dir_x` (FLOAT, default 0.7071, min -1.0, max 1.0, step 0.01), `dir_y` (FLOAT, default 0.7071, min -1.0, max 1.0, step 0.01)."
        },
        {
          "id": "US-005-AC02",
          "text": "`execute()` returns `build_effect_params(\"pan_diagonal\", {\"u_speed\": speed, \"u_origin_x\": origin_x, \"u_origin_y\": origin_y, \"u_dir_x\": dir_x, \"u_dir_y\": dir_y})`."
        },
        {
          "id": "US-005-AC03",
          "text": "Node is listed under `NODE_CLASS_MAPPINGS[\"CoolPanDiagonalEffect\"]` in `__init__.py`."
        }
      ]
    },
    {
      "id": "US-006",
      "title": "Integration tests verify rendered output shape and dtype for all five pan nodes",
      "description": "As a developer, I want pytest tests that exercise the full rendering path for each pan node so that regressions in shader loading or uniform passing are caught automatically.",
      "acceptanceCriteria": [
        {
          "id": "US-006-AC01",
          "text": "`tests/test_pan_effects.py` exists with one test function per pan direction (five total)."
        },
        {
          "id": "US-006-AC02",
          "text": "Each test constructs a 1×4×4×3 float32 torch tensor as input, calls `CoolVideoGenerator.execute()` with the corresponding pan `EFFECT_PARAMS`, `fps=1`, `duration=1.0`."
        },
        {
          "id": "US-006-AC03",
          "text": "Each test asserts the output tensor has shape `(1, 4, 4, 3)`, dtype `torch.float32`, and all values in `[0.0, 1.0]`."
        },
        {
          "id": "US-006-AC04",
          "text": "Tests are skipped (via `pytest.importorskip(\"moderngl\")`) if `moderngl` is not available."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "All five node files must import `build_effect_params` from `effect_params.py` using the same `importlib.util` pattern as the existing effect nodes."
    },
    {
      "id": "FR-2",
      "description": "`RETURN_TYPES = (\"EFFECT_PARAMS\",)`, `RETURN_NAMES = (\"EFFECT_PARAMS\",)`, `FUNCTION = \"execute\"`, `CATEGORY = \"CoolEffects\"` must be set on every node class."
    },
    {
      "id": "FR-3",
      "description": "Each node must be added to both `NODE_CLASS_MAPPINGS` and `NODE_DISPLAY_NAME_MAPPINGS` in `__init__.py` (display names: \"Cool Pan Left Effect\", \"Cool Pan Right Effect\", \"Cool Pan Up Effect\", \"Cool Pan Down Effect\", \"Cool Pan Diagonal Effect\")."
    },
    {
      "id": "FR-4",
      "description": "Adding the five nodes must not break the existing nodes — `CoolGlitchEffect`, `CoolVHSEffect`, `CoolZoomPulseEffect`, `CoolEffectSelector`, and `CoolVideoGenerator` must still load and register correctly."
    },
    {
      "id": "FR-5",
      "description": "Tests must not require a display or GPU — `moderngl.create_standalone_context(backend=\"egl\")` must be the only rendering context used."
    }
  ]
}
