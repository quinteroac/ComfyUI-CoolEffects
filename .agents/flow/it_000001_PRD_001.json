{
  "goals": [
    "Establish the canonical directory layout for the package.",
    "Provide a Python shader loader that reads `.frag` files from the shaders directory.",
    "Provide a JavaScript shader loader that reads the same `.frag` files at runtime via fetch.",
    "Expose a `/cool_effects/shaders` HTTP endpoint listing available shader names.",
    "Ship 3–5 initial GLSL fragment shader effects as working examples.",
    "Register the package with ComfyUI so it is discoverable."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Package Discovery",
      "description": "As a ComfyUI user, I want the ComfyUI-CoolEffects package to load without errors so that I can see it listed in the ComfyUI node browser even before any nodes are wired.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "ComfyUI starts without Python errors when ComfyUI-CoolEffects is present in `custom_nodes/`."
        },
        {
          "id": "US-001-AC02",
          "text": "`__init__.py` exports `NODE_CLASS_MAPPINGS` and `NODE_DISPLAY_NAME_MAPPINGS` (both can be empty dicts initially)."
        },
        {
          "id": "US-001-AC03",
          "text": "ComfyUI server log shows no import errors for this package."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Python Shader Loader",
      "description": "As a backend node developer, I want to call `load_shader(\"glitch\")` and receive the GLSL source string so that I can pass it directly to ModernGL without path manipulation.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`shaders/loader.py` exports a `load_shader(name: str) -> str` function."
        },
        {
          "id": "US-002-AC02",
          "text": "`load_shader(\"glitch\")` returns the full text of `shaders/glsl/glitch.frag`."
        },
        {
          "id": "US-002-AC03",
          "text": "`load_shader(\"nonexistent\")` raises `FileNotFoundError` with a message that includes the shader name."
        },
        {
          "id": "US-002-AC04",
          "text": "The loader resolves paths relative to its own file location, not the CWD."
        },
        {
          "id": "US-002-AC05",
          "text": "`shaders/loader.py` exports a `list_shaders() -> list[str]` function that returns shader names (without extension) sorted alphabetically."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Shader List Endpoint",
      "description": "As a frontend widget, I want to fetch `GET /cool_effects/shaders` and receive a JSON array of shader names so that the effect dropdown is always in sync with shaders on disk without a server restart.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "The endpoint is registered via `PromptServer.instance.routes` at package import time."
        },
        {
          "id": "US-003-AC02",
          "text": "`GET /cool_effects/shaders` returns HTTP 200 with `Content-Type: application/json`"
        },
        {
          "id": "US-003-AC03",
          "text": "The response reflects the actual `.frag` files in `shaders/glsl/` at the time of the"
        }
      ]
    },
    {
      "id": "US-004",
      "title": "JavaScript Shader Loader",
      "description": "As a frontend node developer, I want to `import { loadShader, listShaders } from \"../shaders/loader.js\"` so that I can populate the dropdown and feed GLSL source to Three.js without duplicating code.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`web/shaders/loader.js` exports `listShaders()` — async, fetches `GET /cool_effects/shaders`,"
        },
        {
          "id": "US-004-AC02",
          "text": "`web/shaders/loader.js` exports `loadShader(name)` — async, fetches the `.frag` file"
        },
        {
          "id": "US-004-AC03",
          "text": "If `listShaders()` receives a non-200 response, it throws `Error(\"Failed to list shaders: <status>\")`."
        },
        {
          "id": "US-004-AC04",
          "text": "If `loadShader(name)` receives a non-200 response, it throws `Error(\"Shader not found: <name>\")`."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Initial Shader Effects",
      "description": "As a developer, I want at least 3 working GLSL fragment shaders available so that I can verify the loader and demonstrate the package's range of effects.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "The following shader files exist and compile without GLSL errors when loaded by ModernGL:"
        },
        {
          "id": "US-005-AC02",
          "text": "Each shader accepts: `uniform sampler2D u_image`, `uniform float u_time`,"
        },
        {
          "id": "US-005-AC03",
          "text": "A `shaders/README.md` documents the required uniforms contract for adding new shaders."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "The package root must contain `__init__.py`, `requirements.txt`, and `README.md`."
    },
    {
      "id": "FR-2",
      "description": "The directory `shaders/glsl/` must hold all `.frag` shader files."
    },
    {
      "id": "FR-3",
      "description": "`shaders/loader.py` must locate shader files using `Path(__file__).parent` as the base."
    },
    {
      "id": "FR-4",
      "description": "The `/cool_effects/shaders` endpoint must be registered in `__init__.py` using `PromptServer.instance.routes.get`."
    },
    {
      "id": "FR-5",
      "description": "`web/shaders/loader.js` must use `fetch` to retrieve both the shader list and shader source — no bundling step required."
    },
    {
      "id": "FR-6",
      "description": "Each shader must follow the uniforms contract: `u_image` (sampler2D), `u_time` (float), `u_resolution` (vec2)."
    },
    {
      "id": "FR-7",
      "description": "`requirements.txt` must list `moderngl` as a dependency."
    }
  ]
}
