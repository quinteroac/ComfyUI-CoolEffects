{
  "goals": [
    "Introduce a `CoolTextOverlay` Python node that accepts a VIDEO and a rich text configuration and",
    "Support multiple styled text fragments per overlay (rich inline: each fragment has independent",
    "Allow precise placement via normalised X/Y coordinates and horizontal alignment (left / center /",
    "Keep Pillow as the sole new backend dependency (already present in virtually every ComfyUI",
    "Register the node in `__init__.py` following the existing `importlib` pattern."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Single-style text overlay on video",
      "description": "As a ComfyUI user, I want to connect a VIDEO to `CoolTextOverlay` and specify a text string with a font, size, and color so that every frame of the output VIDEO has that text rendered at the position I specified.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "The node accepts a VIDEO input and returns a VIDEO output of identical resolution, fps, and"
        },
        {
          "id": "US-001-AC02",
          "text": "A widget `text` (STRING) sets the full overlay text when no fragments are provided."
        },
        {
          "id": "US-001-AC03",
          "text": "Widgets `font_family` (STRING, default `\"arial\"`), `font_size` (INT, 24–256, default 48),"
        },
        {
          "id": "US-001-AC04",
          "text": "Widgets `pos_x` (FLOAT 0.0–1.0, default 0.5) and `pos_y` (FLOAT 0.0–1.0, default 0.1)"
        },
        {
          "id": "US-001-AC05",
          "text": "Widget `align` (COMBO `[\"left\",\"center\",\"right\"]`, default `\"center\"`) controls text"
        },
        {
          "id": "US-001-AC06",
          "text": "Widget `opacity` (FLOAT 0.0–1.0, default 1.0) blends the text into the frame."
        },
        {
          "id": "US-001-AC07",
          "text": "Running the node with a 3-frame VIDEO produces an output VIDEO where all 3 frames contain the"
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Rich inline multi-fragment text overlay",
      "description": "As a ComfyUI user, I want to supply a JSON array of text fragments (each with its own `text`, `font_family`, `font_size`, `color`, `font_weight`) so that I can render composite labels (e.g., white normal word followed by a yellow bold word) burned into the video.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "The node accepts an optional `fragments` input of type STRING containing a JSON array; when"
        },
        {
          "id": "US-002-AC02",
          "text": "Each fragment object must support keys: `text` (required), `font_family` (optional, falls"
        },
        {
          "id": "US-002-AC03",
          "text": "Fragments are laid out left-to-right on a single baseline; `pos_x`/`pos_y`/`align` apply to"
        },
        {
          "id": "US-002-AC04",
          "text": "Passing an invalid JSON string raises a `ValueError` with a human-readable message."
        },
        {
          "id": "US-002-AC05",
          "text": "A VIDEO rendered with two fragments of different colors shows each fragment in its respective"
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Pillow font resolution",
      "description": "As a developer, I want the node to resolve font files robustly so that it does not crash on systems where a requested font family is not installed.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "The node attempts to load the font via `ImageFont.truetype`; on `OSError` it falls back to"
        },
        {
          "id": "US-003-AC02",
          "text": "The fallback produces visible text in the output (no silent blank frames)."
        },
        {
          "id": "US-003-AC03",
          "text": "Font resolution is attempted once per unique `(font_family, font_size, font_weight)` tuple"
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "Node class `CoolTextOverlay` lives in `nodes/text_overlay_effect.py`."
    },
    {
      "id": "FR-2",
      "description": "`INPUT_TYPES` returns `{\"required\": {\"video\": (\"VIDEO\",)}, \"optional\": {\"fragments\": (\"STRING\", {\"default\": \"[]\"})}}` plus all style/position widgets."
    },
    {
      "id": "FR-3",
      "description": "`RETURN_TYPES = (\"VIDEO\",)`, `RETURN_NAMES = (\"video\",)`, `FUNCTION = \"execute\"`."
    },
    {
      "id": "FR-4",
      "description": "`execute()` iterates over every frame tensor in the VIDEO payload, converts each to a"
    },
    {
      "id": "FR-5",
      "description": "For rich inline fragments, each fragment is drawn sequentially; the X cursor advances by"
    },
    {
      "id": "FR-6",
      "description": "Opacity blending uses `Image.paste` with an RGBA mask derived from `opacity`."
    },
    {
      "id": "FR-7",
      "description": "GL resource cleanup pattern does NOT apply here (no ModernGL); Pillow images are created"
    },
    {
      "id": "FR-8",
      "description": "Node is registered in `__init__.py` under the key `\"CoolTextOverlay\"` with display name"
    },
    {
      "id": "FR-9",
      "description": "`requirements.txt` gains a `Pillow` entry if not already present."
    }
  ]
}
