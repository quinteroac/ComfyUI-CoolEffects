{
  "goals": [
    "Provide a `CoolTextOverlayEffect` node that outputs `EFFECT_PARAMS` consumable by `CoolVideoGenerator`.",
    "Allow users to configure text content, appearance (font size, color), and screen position.",
    "Support at least two animation styles (e.g. fade-in and slide-in) so text enters the frame dynamically.",
    "Maintain visual consistency with existing effect nodes: live WebGL2 preview in the node widget."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Configure text appearance",
      "description": "As a content creator, I want to type my text and set its font size and color so that I can match the overlay to my video's visual style.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "Node exposes a `text` STRING input (free text, default `\"Hello World\"`)."
        },
        {
          "id": "US-001-AC02",
          "text": "Node exposes a `font` COMBO input populated at load time by scanning `assets/fonts/` for `.ttf` files; defaults to the first font found alphabetically."
        },
        {
          "id": "US-001-AC03",
          "text": "Node exposes a `font_size` INT input (default `48`, min `8`, max `256`)."
        },
        {
          "id": "US-001-AC04",
          "text": "Node exposes `color_r`, `color_g`, `color_b` FLOAT inputs (0.0–1.0, default white `1.0, 1.0, 1.0`)."
        },
        {
          "id": "US-001-AC05",
          "text": "Node exposes an `opacity` FLOAT input (0.0–1.0, default `1.0`)."
        },
        {
          "id": "US-001-AC06",
          "text": "If `assets/fonts/` is empty or missing, node raises a clear `ValueError` at load time."
        },
        {
          "id": "US-001-AC07",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Position text on the image",
      "description": "As a content creator, I want to choose where the text appears on the frame so that I can place titles at the top, captions at the bottom, or watermarks in a corner.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "Node exposes a `position` COMBO input with options: `top-left`, `top-center`, `top-right`, `center`, `bottom-left`, `bottom-center`, `bottom-right` (default `bottom-center`)."
        },
        {
          "id": "US-002-AC02",
          "text": "Node exposes `offset_x` and `offset_y` FLOAT inputs (−1.0–1.0, default `0.0`) for fine-grained nudging from the anchor point."
        },
        {
          "id": "US-002-AC03",
          "text": "The chosen position is reflected correctly in the live WebGL2 preview."
        },
        {
          "id": "US-002-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Animate the text",
      "description": "As a content creator, I want to choose an animation style and control its duration so that the text enters the frame in an engaging way rather than appearing abruptly.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "Node exposes an `animation` COMBO input with options: `none`, `fade_in`, `fade_in_out`, `slide_up`, `typewriter` (default `fade_in`)."
        },
        {
          "id": "US-003-AC02",
          "text": "Node exposes an `animation_duration` FLOAT input (seconds, default `0.5`, min `0.0`, max `5.0`) controlling the duration of the animation phase."
        },
        {
          "id": "US-003-AC03",
          "text": "With `none`: text is visible at full opacity for the entire clip."
        },
        {
          "id": "US-003-AC04",
          "text": "With `fade_in`: text transitions from transparent to the configured opacity over `animation_duration` seconds; stays visible for the remainder of the clip."
        },
        {
          "id": "US-003-AC05",
          "text": "With `fade_in_out`: text fades in over the first `animation_duration` seconds and fades out over the last `animation_duration` seconds of the clip."
        },
        {
          "id": "US-003-AC06",
          "text": "With `slide_up`: text enters from below its anchor position, sliding up into place over `animation_duration` seconds."
        },
        {
          "id": "US-003-AC07",
          "text": "With `typewriter`: characters are revealed one by one from left to right over `animation_duration` seconds; because GLSL cannot iterate over string characters, the typewriter effect is implemented on the Python/Pillow side by rendering a substring of increasing length per frame (this requires per-frame texture regeneration for this animation only)."
        },
        {
          "id": "US-003-AC08",
          "text": "Animations are driven by `u_time` (and clip total duration via `u_duration` uniform) so they work correctly across all frame rates."
        },
        {
          "id": "US-003-AC09",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Output EFFECT_PARAMS compatible with CoolVideoGenerator",
      "description": "As a content creator, I want the node to produce an `EFFECT_PARAMS` output so that I can chain it with other effects inside `CoolVideoGenerator` and render an animated video with text.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "Node `RETURN_TYPES = (\"EFFECT_PARAMS\",)`."
        },
        {
          "id": "US-004-AC02",
          "text": "`execute()` calls `build_effect_params(\"text_overlay\", {...})` with all configured parameters."
        },
        {
          "id": "US-004-AC03",
          "text": "Node is registered in `NODE_CLASS_MAPPINGS` as `\"CoolTextOverlayEffect\"` and in `NODE_DISPLAY_NAME_MAPPINGS`."
        },
        {
          "id": "US-004-AC04",
          "text": "A `CoolVideoGenerator` workflow with `CoolTextOverlayEffect` connected renders a video with visible animated text."
        },
        {
          "id": "US-004-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Live WebGL2 preview in the node widget",
      "description": "As a content creator, I want to see the text overlay rendered in real time inside the node so that I can adjust parameters without running the full workflow.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "A JS extension `web/text_overlay_effect.js` is created, following the pattern of `web/zoom_in_effect.js` / `web/effect_node_widget.js`."
        },
        {
          "id": "US-005-AC02",
          "text": "The WebGL2 canvas widget renders a sample image with the configured text overlay and animation applied."
        },
        {
          "id": "US-005-AC03",
          "text": "Changing any node input (text, font size, color, position, animation) updates the preview in real time."
        },
        {
          "id": "US-005-AC04",
          "text": "Visually verified in browser: text appears at the correct position with the selected animation playing on the canvas."
        },
        {
          "id": "US-005-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "A new Python node file `nodes/text_overlay_effect.py` implementing `CoolTextOverlayEffect`. At class load time it scans `assets/fonts/` (relative to the package root) for `*.ttf` files and exposes them as a COMBO input; the selected font name is passed as a parameter so `video_generator.py` can resolve the full path via `assets/fonts/<font_name>` when rendering."
    },
    {
      "id": "FR-2",
      "description": "A new GLSL fragment shader `shaders/glsl/text_overlay.frag` accepting `u_image`, `u_time`, `u_resolution`, and all text-related uniforms; composites the text layer over the base image with the selected animation."
    },
    {
      "id": "FR-3",
      "description": "Because GLSL cannot render arbitrary TrueType text natively, the text is pre-rendered to a texture via Pillow and uploaded to the shader as a second sampler uniform `u_text_texture`. For all animations except `typewriter`, the texture is rendered **once** before the frame loop and reused. For `typewriter`, the texture is re-rendered **per frame** with an increasing substring length. `CoolVideoGenerator` must be extended to handle both strategies for the `text_overlay` effect."
    },
    {
      "id": "FR-4",
      "description": "The JS extension must load `text_overlay.frag` via `loadShader(\"text_overlay\")` and use `mount_effect_node_widget` (from `web/effect_node_widget.js`) to mount the live preview canvas."
    },
    {
      "id": "FR-5",
      "description": "Node registered in `__init__.py` using `importlib.util.spec_from_file_location`, following the same pattern as all existing effect nodes."
    },
    {
      "id": "FR-6",
      "description": "`NODE_CLASS_MAPPINGS[\"CoolTextOverlayEffect\"]` and `NODE_DISPLAY_NAME_MAPPINGS[\"CoolTextOverlayEffect\"] = \"Cool Text Overlay Effect\"`."
    }
  ]
}
